"use client"; // Marcar como Cliente

import React from 'react';
import LoginRegister from '../components/LoginRegister'; // Asegúrate de que la ruta sea correcta

export default function LoginPage() {
  return (
    <div>
      <LoginRegister />
    </div>
  );
} 